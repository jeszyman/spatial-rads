# Repo cleanup items

- [ ] `config/typing_provenance.md`: restructure as machine-readable YAML (scVI hyperparameters, seeds, environment versions, validation baseline counts)
- [ ] CLAUDE.md "Autonomous VM Runs" section: remove (all companion files deleted)
- [ ] `scripts/aggregate/smide_validation.R`: delete (superseded by `smide_de.R`)
