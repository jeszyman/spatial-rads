# spatial-rads

Spatial transcriptomics analysis of microbeam radiation therapy (MBRT) using CosMx single-cell data from the Mutter Lab (Mayo Clinic).

## Scientific Context

First-in-field spatial transcriptomics study of MBRT peak/valley biology. MBRT delivers dose through ~1mm-spaced parallel beams creating alternating high-dose peaks and low-dose valleys. Key question: do peaks show direct damage signatures while valleys show immune priming / bystander effects, and how does this compare to uniform SBRT?

## Datasets

> **M02 timepoint relabel APPLIED (2026-08-13).** Run 2 is a 2×2 (slides 1-2 = 2d, slides 3-4 = 4h), not day-2-only. `build_metadata_xlsx.R` now reads `mutter02_block_layout.csv` for per-block timepoints. Pipeline fixed per `plan-run2-4h-relabel-reanalysis.md` Phases 0-5: metadata corrected, comparisons registry expanded (combined_4h_treated n=3v3, combined_4h n=3v2, mutter02_day2 n=2/arm), engine cohort-sample whitelist (`cohort_samples.tsv`) added to prevent cross-dataset condition-name leakage. Day-2 n was 4/arm (contaminated) → 2/arm (corrected); SBRT fibrosis signal survives and strengthens. New combined 4h results: Cdkn1a (p21) MBRT > SBRT across 5 cell types (padj < 0.05, per-cell_type BH; 10 total MBRT_vs_SBRT hits). Arm tables use per-cell_type BH (field-standard per Crowell et al. 2020 muscat). Tables at `results/aggregate/combined_4h/` and `results/aggregate/mutter02_day2/`. Some references below (hit counts, n=4 MDE, old master row counts) reflect the pre-correction state; consult the corrected engine outputs directly.

- **Mutter_01** (current): 11 conditions (MBRT/SBRT/Control × timepoints), 971K cells post-QC, CosMx 1000-gene Mouse UCC panel. Input parquets at `/mnt/data/projects/spatial-rads/inputs/mutter01/` Raw per-slide Seurat RDS (negprobes+falsecode assays) also at inputs/mutter01/; additive per-cell control sidecar at /mnt/data/projects/spatial-rads/processing/cell_controls.parquet + flag-only per-FOV falsecode QC (results/processing/fov_falsecode_qc.tsv); negprobe-mean and RNA counts verified bit-identical to the parquet across all 4 slides. **Adapter re-base executed 2026-06-22**: `adapt_mutter01.R` now ingests the 4 per-slide RDS for counts (verified byte-identical across all 11 samples; the metadata parquet is retained as the Condition + Yi-label side-input) and the data model is `format=rds`. Output is invariant so the locked aggregate is unchanged; `recover_negprobes` retirement + the frozen-seed aggregate rebuild remain deferred.
- **Mutter_02**: 4 slides, 12 samples (3 per slide: Control/MBRT/SBRT; slides 1-2 at 48h, slides 3-4 at 4h — a 2×2 timepoint design, not day-2-only), 2.35M cells post-QC, 972-gene Mouse UCC panel (base + Mayo/Thompson-Fields custom add-on). Raw Seurat RDS at `/mnt/data/projects/spatial-rads/inputs/mutter02/`. Preprocessed via `preprocessing.smk` (sample assignment, QC, LogNormalize; per-sample typing/pathway removed 2026-07-10). Historically (2026-05-30) also cell-typed via Seurat anchor TransferData against a pooled M01 cell reference (replaces InSituType, which failed on cross-dataset batch shift in this per-sample application; note the `aggregate_typing.smk` workflow re-types all cells at merged scale via scVI integration + cluster-then-annotate, a different application that supersedes these per-sample labels for cross-dataset work — see Analysis Architecture), and UCell/AddModuleScore pathway scoring.

## Analysis Architecture

**Invariant preprocessing pipeline (per-sample / pre-aggregate)** — `workflows/preprocessing.smk` (tangled from org; split from the former `processing.smk` on 2026-07-10). Adapter (raw per-slide RDS → common 950-gene panel Seurat) → 4-criteria QC filter → LogNormalize, plus **report-only QC** (probe-QC diagnostic; `contamination_qc` SpatialQM MECR marker-bleed per-sample + per-FOV with necrosis/mixed-biology triage cols, FastReseg infeasible — no transcript coords; control sidecar; `sample_metrics` per-sample SpatialQM technical QC → `sample_tech_metrics.tsv` = TPC/features/sparsity/SNR/specificityFDR). **Per-sample terminus = `norm.rds`.** Per-sample cell typing + pathway scoring were **deleted** (canonical typing is cohort-scale in `aggregate_typing.smk`, which re-types every cell at merged scale).

**Per-sample typing removed (2026-07-10).** The staged v2.0 de-anchored per-sample typing (`scripts/celltype.R`) and the v1 typing it would have replaced are both retired — per-sample cell typing + pathway scoring were deleted with the preprocessing split. Canonical labels come from the merged-scale `aggregate_typing.smk` typing (→ `results/aggregate/full_labels.parquet`). `config/lineage_markers.yaml` is retained (now consumed by `contamination_qc` + aggregate tier-2 stroma typing); `config/pathway_gene_lists.yaml` retained (consumed by `data_model.smk` gene-set build + aggregate pathway scoring).

**Cross-sample workflow (`aggregate_typing.smk` + `aggregate_differential.smk`, split 2026-07-12 at the label-handoff seam)** — cell typing **executed and locked 2026-06-02**; current design/status in `docs/plans/plan-aggregate.md` (v2.5 banner). Cross-dataset typing assigns identity **once on the merged 3,277,090-cell, 950-gene object** (no M01→M02 transfer; typing is structural). Coarse typing is **cluster-then-annotate, not per-cell**: an initial per-cell InSituType run failed validation (84.6% mislabeled `tumor_epithelial`, immune gold-marker recall <5%) because the per-cell *unit of inference* is unreachable at this panel sparsity (median 58/950 genes per cell) — that was the root cause, NOT the tumor anchor (memory `project_insitutype_aggregate_failure.md`). **Tier-1 (coarse):** scVI integration [Lopez 2018] (30 latent dims, 2 layers, NB likelihood, `batch_key=slide_id`, per-cell negprobe as continuous covariate, 50 epochs, GPU-direct on the RTX A4000 — scVI replaced Harmony, which 2.0.2-LAPACK-errors on ≥2 covariates) → 15-NN graph → **Leiden** [Traag 2019] (`flavor="leidenalg"`, `n_iterations=2`; igraph over-partitions, run-to-convergence intractable at 3.27M nodes) with a swept resolution (0.5 failed premise; 1.0/2.0/3.0 swept; **res=3.0 adopted**, finest stable partition) → cluster-level marker annotation (≥100 cells + signal-to-background ≥2). All four pre-registered Q4 gates PASSED (premise; batch-mixing by imbalance-scaled iLISI 0.67 vs 0.5 floor, ceiling 1.70 for the 29/71 split, per-cluster minority demoted to a QC flag; marker-recall; negprobe QC). Result: **tumor 48.1% (1,577,685) / stroma 40.6% (1,331,601) / immune 11.2% (367,800), 4 unassigned.** **Tier-2 (immune):** cluster-level **SingleR** [Aran 2019] vs celldex **ImmGen** (label.main) on the 367,800 immune cells (subtyped at res=3.0; rare lineages re-examined at res=6.0). SingleR assigns no B/Plasma/Neutrophil class at this sparsity, so a **marker-rescue step** (`tier2_immune_rescue.py` — the Mast-rule "marker evidence overrides reference confidence", precedent Cheng et al. BMC Bioinformatics 2025 Xenium benchmark) overrides SingleR where a res=6.0 subcluster's **identity markers** (lineage-defining, non-secreted, non-shared subset of each panel) win by argmax AND ≥2 corroborate. Recovers **Plasma 3,573** (the SingleR-"DC" subcluster 21: Mzb1 17.8×/Xbp1 19.7× bg) and **Neutrophils 259** (subcluster 272: Elane/Prtn3/Mpo, S100a8 260×); only 3,832 cells (1.0% of immune) reassigned. A plain signal-to-background argmax was rejected for *assignment* (it stays the detectability *gate*): ambient immunoglobulin (Jchain/Igkc) and non-specific genes (Cd37 pan-leukocyte, Xbp1 secretory) corrupt set-means — naive argmax falsely flipped an 8k T-cell subcluster (Cd3e 44×) to Plasma. **Final immune: macrophages 300,975 / ILC 34,340 / T 16,046 / DC 5,315 / NK 4,991 / Plasma 3,573 / Mast 1,121 / Neutrophils 259 (+1,180 epithelial-contamination).** **B cells confirmed near-absent**: 0/298 res=6.0 subclusters have B as the argmax identity (B's signal always rank ≥2); B markers on-panel but diffuse (2.4–3.3% of cells carry any B count). (An in-session "B RETAIN" reading was an artifact of per-lineage-best detectability output — B's panel clears background at subcluster 3, but that subcluster is dominantly Plasma/T — corrected here.) Final labels at `aggregate/full/immune_subtypes_rescued.parquet` (pre-rescue SingleR-only in `immune_subtypes.parquet`). These labels **supersede the per-sample TransferData labels** for all cross-dataset analysis. **Tier-2 (stroma):** UCell cluster-level argmax+margin (`tier2_stroma_ucell.R`) against the `config/lineage_markers.yaml` stromal sets (NOT SingleR/MouseRNAseqData, whose bulk reference lacks pericyte/smooth-muscle classes) → Fibroblast 796,933 / SmoothMuscle 73,570 / unassigned 461,098 (pre-rescue UCell). A **marker-rescue step** (`tier2_stroma_rescue.py`, the same identity-marker override built for immune, examining r6 subclusters) then recovered the minor lineages UCell's fibroblast-washed rank score missed but the detectability gate confirmed present: **Endothelial 36,937** (its best subcluster 8 had been 99.5% mislabeled Fibroblast by UCell; anchored by Cdh5/Pecam1) and **Adipocyte 49,214** (anchored by lipid-droplet Cidea; present in BOTH cohorts so not a single-dataset artifact). A **specificity anchor** (≥1 lineage-exclusive marker must individually clear the gate) blocks shared-gene false flips — Fabp4 is adipocyte+endothelial+macrophage (s2b 254 on the adipocyte cluster but also 39 on the endothelial one), so a Fabp4-driven call with no Cidea is rejected; this caught 2 false subclusters (Cidea 0.83/1.96). 86,151 cells (6.5% of stroma) reassigned, only out of Fibroblast/SMC/unassigned. **Pericyte stays 0** (best subcluster 8 shared with and dominated by Endothelial; only 3 panel markers, Pdgfrb fibroblast-shared — present but unresolvable de novo, folded into the perivascular endothelial call). **Final stroma: Fibroblast 732,099 / stroma_unresolved 441,227 / SmoothMuscle 72,124 / Adipocyte 49,214 / Endothelial 36,937.** Final labels at `aggregate/full/stroma_subtypes_rescued.parquet` (pre-rescue UCell-only in `stroma_subtypes.parquet`). **Unified per-cell label table** joining all three tiers (`unify_labels.py` → `results/aggregate/full_labels.parquet`, 3,277,090 rows; `compartment` + final `cell_subtype`) is the single canonical input for downstream analysis. **Differential layer (complete, 2026-06-03)** — `workflows/aggregate_differential.smk` runs the MBRT-vs-SBRT-vs-Control downstream analysis on these unified labels: three pre-registered confirmatory hypotheses (H1 immune activation, H2 vascular/oxygen, H3 stromal) across three contrasts (MBRT_vs_Ctrl, SBRT_vs_Ctrl, MBRT_vs_SBRT) via composition (propeller), pseudobulk DE + GSEA, UCell scores over the 8 curated `config/pathway_gene_lists.yaml` sets, kNN-composition Mclust niches (k=50 NN, SPIN 50% subsampling, BIC-selected K; upgraded from k=20/K=6 k-means per CosMx best practices 2026-08-13), Keren immune-mixing (k=50 NN), myeloid M1/M2 polarization, and M01↔M02 effect-size concordance. M02 day-2 carries formal inference (n=2/arm after relabel correction, `~0+condition+slide_id`); combined 4h carries MBRT_vs_SBRT inference (n=3/arm, `~0+condition`); M01 is n=1 descriptive. Inference rests on effect size + 95% CI + n=4 MDE with tiered FDR (confirmatory family BH-pooled → `padj_confirmatory`; exploratory keeps own BH); no volcano plots (invalid on sparse ~950-gene CosMx). Single canonical output `results/aggregate/results_master.tsv` (32,961 tier-tagged rows; 621 confirmatory / 32,340 exploratory). Realized result: day-2 signal is SBRT-driven stromal fibrosis (collagens + Acta2; H3 = 20 hits SBRT_vs_Ctrl, padj_confirmatory to ~2e-11); MBRT essentially null at whole-compartment scale (MBRT_vs_Ctrl 2/207 confirmatory hits, 207/207 below n=4 MDE); MBRT_vs_SBRT confirms SBRT>MBRT for the same stromal genes (19/20 hits H3). 578/621 (93%) confirmatory rows fell below their n=4 MDE, so most MBRT non-results are underpowered, not true nulls (caveat: M02 MBRT dose unrecorded; pseudobulk spatially dilutes any peak/valley-localized MBRT effect). Extends the prior weak/null MBRT-immunostim finding (memory `project_mbrt_mechanism_status`). **Robustness pass 2026-06-23 (`plan-differential-robustness.md` + `-impl.md`, after a 4-lens devils-advocate panel + a deep-solo spec critique):** the day-2 signal is recontextualised as **cell-state composition shifts, not per-cell regulation** — every DE row now carries a detection-vs-level `call_class` (limma-eBayes on per-sample expressing-fractions + per-expresser level, n=4, BH within cell_type×contrast, symmetric level floor = 2 counts). The SBRT collagen/Acta2 program classifies `fraction_shift` (more cells expressing; per-expresser level n.s.); the pre-registered fibroblast resting/activated split (gate-passed: 313,088 activated / 419,011 resting, Col5a1 specificity anchor 2.6× confirms fibroblast-specific not SMC-bleed) shows the activated-fibroblast proportion trends up in SBRT (+0.51 logit, padj 0.59 n.s. at n=4) — fibrosis is **myofibroblast expansion (composition), underpowered to confirm**, not per-cell upregulation. SBRT tumor shows Ccnd1/Hmgb2/Mki67↑ with p21↓ (proliferative regrowth of p53-mutant 4T1 survivors, NOT arrest — revises the old "MBRT p21 = arrest" reading). p21/Cdkn1a reclassified `ambiguous` per cell type (detection drops but ~1.4-count level too sparse to separate; the earlier confident "fraction shift" was a pooled-log-norm artifact). 33% stroma_unresolved shown non-confounding (arm-stable fraction, no call flips); STING demoted (2/3 on-panel genes shared with the IFN sets). Strong original DESeq2/pathway hits **recontextualised as composition, not overturned**; MBRT still null at whole-compartment scale. Plumbing: UCell compute split from plotting (`pathway_scores.R`/`pathway_plots.R`) so a plot bug cannot roll back the recompute. New scripts: `scripts/aggregate/{detection_test.R,substate_split.R,substate_composition.R,geneset_overlap.R}` + `config/substate_markers.yaml` (pre-registered on-panel markers). Design/decisions in `plan-mbrt-vs-sbrt.md`; **framing reframed 2026-06-22 in `plan-mbrt-vs-sbrt-reanalysis.md`** (after a 4/4 devils-advocate panel) — the MBRT arm-level "null" is **spatial dilution, not a biological null**: the M01 4h peak/valley work found a peak-localized MBRT signature (37 genes: MHC-I/IFN/damage) that whole-compartment averaging cancels. **RESOLVED (2026-07-13): the 37-gene signature is an artifact; do NOT cite the M01 4h peak signature as established.** `plan-rotation-null-reconcile.md` (Steps 0-2, executed on beast) settled it: the rotation null was never the culprit. Rerunning the rotation-null statistic itself reproduces 38 survivors on the retired ImmGen `a` bucket, collapses to 7 after swapping to current atlas tumor cells + current QC (the IFN module Ifitm2/3/Ifi27/Oas1a-g falls to zero-spanning bootstrap CIs; MHC-I splits: H2-D1 holds, B2m shrinks, H2-K1 collapses), and drops to **0 genome-wide survivors** once aggregated to the FOV replicate unit (paired `~ FOV + zone` DESeq2, 67 paired FOVs, plus a continuous phase regression at the FOV unit; flat p-histogram, frac p<0.05 = 0.046). So the signature dies in two stages: the cell-label + QC swap kills the inflammatory program, then pseudoreplication kills the residual. A phase null was shown geometrically futile up front (tumor spans ~4.9 beam periods, so p-floor ~0.2-0.47) and skipped. Lone near-miss Clu (within-FOV peak-up 0.23, padj 0.084, null phase slope) is a single-gene lead, not the claimed MHC-I/IFN program; proliferation genes trend peak-DOWN. This confirms the earlier whole-tumor FOV-pseudobulk null and supplies the mechanism it lacked. Figure `dev/if_channel_check/pv_reconcile_figure.png`. See `** Results` (Post-IR dynamics) and memory `project_mbrt_mechanism_status`. The reanalysis keeps all 3 contrasts effect-size-forward + symmetric panel coverage + a detectability table, never says "sparing" (MBRT dose unrecorded), and gates the resolving within-slide spatial analysis on M02 peak/valley staining (absent). Conda: `spatial-rads-scvi` (Python: scvi-tools/scanpy/leidenalg/GPU torch) for tier-1, `spatial-rads` (R: SingleR/celldex, needs `scrapper`) for tier-2. Tier scripts: `scripts/aggregate/{full_export.R,full_cluster.py,finalize_tier1.py,tier2_immune_subcluster.py,tier2_singler.R,tier2_immune_rescue.py,tier2_stroma_subcluster.py,tier2_stroma_ucell.R,tier2_stroma_rescue.py,tier2_detectability.py,bcell_diagnostic.py,unify_labels.py}`. Published-QC helpers vendored (pinned to SpatialQM commit 36e1a59d4ca3): `spatialqm_metrics.R` (`sqm_mecr` sample-level segmentation-contamination QC; `sqm_pseudobulk_cor` immune-vs-ImmGen cross-check) — `tier2_detectability.py` is the per-lineage signal-to-background gate (= SpatialQM `getMeanSignalRatio`, cited not re-implemented). **Published-QC confound check (2026-07-10):** the day-2 `fraction_shift` result is guarded by SpatialQM per-sample technical metrics (sensitivity TPC/features/sparsity, SNR, specificityFDR — vendored not installed, extending `spatialqm_metrics.R` with `sqm_sparsity`/`sqm_snr`/`sqm_specificity_fdr`) computed report-only in `preprocessing.smk` (`sample_tech_metrics.tsv`), then compared across arms + a replicate-reproducibility check in `aggregate_differential.smk` (`agg_qc_arm_balance`, `agg_qc_reproducibility`). Result: **5/6 metrics balanced across Control/MBRT/SBRT** (SBRT neither more sensitive nor more contaminated; the lone FDR flag immaterial — SBRT is cleanest), per-arm pseudobulk concordance 0.94–0.97 with **0 outlier slides** and no arm-separating PCA axis → the composition shift is not a QC artifact. Entropy/complexity/dynamic-range dropped (Tier-C + BioQC/gene-order hazards); TPN/FTC not computable (no transcript coords). **Typing-coherence QC (2026-07-13):** the locked labels also carry a **transcriptional neighborhood-purity** report (Plummer Fig 4d; `agg_celltype_purity` in `aggregate_differential.smk` → `celltype_neighborhood_purity.py` → `celltype_neighborhood_purity.tsv`, figure `fig_qc_neighborhood_purity.R`): per-cell fraction of k=30 scVI-latent neighbors sharing the final `cell_subtype`, reusing the existing tier-1 latent (no retype, no GPU), complementary to the marker-recall gate. Purity tracks positively with subtype abundance (Spearman ρ 0.82 over per-subtype medians; k-robust ρ 0.99 for k=15 vs 30): Tumor 0.90 and SmoothMuscle 0.87 highest, the rarest marker-rescued subtypes lowest (Neutrophils 0.10, DC 0.13, Mast 0.13, Plasma 0.23), with SmoothMuscle and NK as visible counterexamples so it reads as association not rule. This is NOT a "sparse lymphocyte" story: NK and T sit mid-pack. **Sample-quality figure suite (2026-07-13):** the notebook `*** Sample quality` header now presents a four-part QC arc (per-sample technical metrics, then cross-arm balance, then replicate reproducibility, then provider concordance), plus a separate `*** Per-cell panel detection` Results header (median 58/950 genes/cell = 6.1%, by compartment×dataset×arm against the 950-gene ceiling; the platform-sparsity figure motivating cluster-then-annotate). Architecture: the pipeline-wired QC steps (`agg_qc_arm_balance`, `agg_qc_reproducibility`, new `agg_qc_panel_sparsity`) emit ONLY tabular output, and plotting moved to standalone `scripts/fig_*.R` (source `~/repos/science/R/figure_schema.R`, `theme_scifig`+`save_plot` dual PNG/SVG, matching the Experimental Design figures; NOT snakemake-wired, run by hand). Two findings surfaced from `sample_tech_metrics.tsv` (memory `project_m01_sensitivity_gradient.md`): (1) the five QC metrics collapse to one axis, per-cell capture sensitivity (TPC~SNR ρ 0.89, TPC~FDR ρ −0.89), so the M01-vs-M02 gap is a single cohort difference (M02 ~1.5-2× deeper-sampled; lower specificity-FDR follows arithmetically) that attenuates M01↔M02 concordance but does NOT confound within-M02 arm contrasts; (2) **M01 capture sensitivity declines monotonically with harvest timepoint (TPC~timepoint ρ −0.98, both arms parallel)**, so M01 temporal trends should be read on within-timepoint contrasts, not across-time magnitude (trajectory shape is capture-confounded). **Workflow reproducibility, refactor planned 2026-06-22:** the locked typing above is hand-run, NOT reproducible from `aggregate_typing.smk` — the wired typing rules are the dead per-cell InSituType chain, and `results/aggregate/full_labels.parquet` + `{AGG}/full/obs.parquet` are orphan inputs (the full-run scVI step is untracked code, no `full_scvi.py`). Harmonized fix in `plan-aggregate-refactor.md` (decisions resolved 2026-06-22: **A2 full re-wire / decision-forcing best-practice review / C1 orchestration-only literate**) — seven steps: audit → method shoot-out (scVI vs scANVI/scArches) → wire the ~13-node multi-env DAG (author `full_scvi.py`, retire `recover_negprobes` onto the re-based M01 RDS) → full rebuild → re-validate the 4 Q4 gates → refresh `results_master.tsv` → literate transfer; the rebuild regenerates the labels and refreshes the differential outputs, accepted by re-validation not byte-identity.

**Differential modularization (BUILT + validated 2026-07-13)**. `aggregate_differential.smk` was refactored from a monolith (`m02day2` plus the 3 contrasts plus `~0+condition+slide_id` copy-pasted across scripts) into three tiers decoupled at two fixed table schemas. **Two test engines** read contrast levels/formula/unit/transform from the resolved registry (`results/data_model/comparisons.tsv`, extended by `build_comparisons.R` to emit `contrast_num_level`/`contrast_den_level`/`ref_level`/`transform`) and emit sufficient statistics only (`comparison,contrast,unit,feature_type,feature_id,estimate,se,df,stat,p`), never adjusted p: `scripts/engines/count_engine.R` (NB pseudobulk DESeq2, apeglm two-fit preserved via one fit per registry reference level) and `scripts/engines/lm_engine.R` (limma with a transform switch: `logit`-via-`speckle::getTransformedProps` for proportions, `identity` for continuous; per-readout `robust` eBayes flag from `config/engine_params.yaml`, where the study-wide thresholds also live). **Feature-producers** in the study layer (`composition.R`, `niches.R`, `spatial_mixing.R`, `myeloid_polarization.R`, `substate_composition.R`) emit a standard per-cell-label or sample×feature table and delegate the arm test to an engine rule; `pathway_scores.R` stays inline (stratified per-cell_type×score_type fit, the documented exception). **Results tier** (`assemble_results.R`) is now the single correction site: it joins the engine outputs, assigns H1/H2/H3, and recomputes every `padj_*` from raw p (reproducing each source's BH grouping). Each engine/producer was verified numerically against its pre-refactor output (composition 45/45, DE 28,686 rows bit-identical, mixing/myeloid/niche/substate exact); the full convergent run passed run-once scientific validation (`scripts/engines/tests/validate_master.R`): the 32,856-row master reproduces SBRT stromal fibrosis, MBRT arm-level null, and p21-ambiguous. `deg_pseudobulk.R` retired (superseded by `count_engine.R`; gsea plus concordance repointed, master byte-identical). Plan at `docs/superpowers/plans/2026-07-12-differential-modularization.md`; design doc in the same specs dir. Not built: a third engine for the peak/valley rotation-null test (out of scope, H2AX-gated); the registry `resolution` (whole vs compartment) axis. See memory `project_differential_modularization`.

**Differential results-table REWORK (BUILT + validated 2026-07-17, two plans; memory `project_results_master_rework`, spec/plans in `docs/superpowers/{specs,plans}/2026-07-14-differential-rework*`).** Supersedes the H1/H2/H3-hardcoded, detection/level-decomposition arrangement above. **Pre-registration is now data, not code:** `config/hypotheses.yaml` (6 frozen hypotheses: `immune_activation`/`vascular_hypoxia`/`stromal_fibrosis` renamed from H1/H2/H3, plus new `myofibroblast_expansion`/`normal_vessel_sparing`/`myeloid_M2`; biological defn + provenance bibkeys, all verified real) + `config/confirmatory_claims.yaml` (frozen results-tier map: which readout x cell_type x program is each hypothesis's a priori evidence). `assemble_results.R` tags confirmatory via a **claims-JOIN** (`scripts/aggregate/load_claims.R`: `load_claims`/`apply_claims`/`assert_no_multiclaim`); producers emit hypothesis=NA, the join fills it. No retro pattern-match, no hardcoded IMMUNE/STROMA_DE/H*_PROG vectors. **FDR:** pooled tiered-BH stays PRIMARY (`padj_confirmatory`); **gatekeeping** (`gate_program`/`gate_padj`/`padj_gated`, GSEA parent gates its DE-gene children) plus **IHW** (`padj_ihw`, keyed log10(baseMean+1)) are AUXILIARY columns in `scripts/aggregate/fdr_helpers.R` (pure sourceable module). Measured: gatekeeping-as-replacement LOSES hits (86 vs 100 pooled), because a closed gate drops genes pooled BH passes, so it is a secondary view, never claimed to control a novel error rate. **Retired:** the bespoke detection/level decomposition (`detection_test.R` deleted, columns dropped) since composition-vs-regulation is unanswerable per-gene at ~1 count/expresser (both a false binary and a statistically ill-posed level test). **Replaced by:** (a) **muscat differential-detection** (`scripts/aggregate/dd_{prep,muscat}.R`, readout_class="detection", the honest "fraction expressing changed" descriptor, always exploratory) run in a **biocontainer** (`apptainer exec --bind /mnt/data <MUSCAT_SIF>`; muscat is conda-unsolvable and BiocManager-source-broken here; smk `dd_muscat` rule uses a `container:` directive; see memory `reference_bioconductor_biocontainer`); (b) **propeller substate composition** wired in (`substate_engine.tsv` into master), making `myofibroblast_expansion` confirmatory (activated-fibroblast +0.51 logit, n.s. n=4). **limma-voom cross-check** (`voom_crosscheck.R`) confirms DESeq2 log2FCs method-robust (median Spearman 0.95 across 42 strata). **Schema fixes:** `n_per_arm` split into true per-arm-n (4) plus `n_samples_used`; `Tumor`/`Epithelial cells` alias collapsed (gated in `verify_arm_tables.R`); `padj_exploratory` dropped. **UCell maxRank=136** (was default 1500, invalid on a 950-gene panel; 136 = largest-signature floor), but arm-level UCell pathway is an **honest null** (tiny +/-0.01-0.03 effects, 0 sig at any maxRank); GSEA (pseudobulk ranking, 78 hits) remains the working pathway readout; M1/M2 now scored (myeloid_M2 pathway evidence, n.s.). Final master 59,214 rows, reproduces SBRT stromal fibrosis plus MBRT arm-level null. **`results_master.tsv` is UNTRACKED from git** (15MB regenerable output, lives on disk only). **merged.rds cohort = 20 samples** (sam0009/0010/0011 dropped before typing, absent from locked labels).

**CosMx best-practice alignment (2026-08-13, branch `worktree-feat-cosmx-bestpractice`; plan at `docs/plans/2026-08-13-cosmx-bestpractice-alignment.md`).** Audited the differential pipeline against NanoString CosMx Analysis Scratch Space recommendations and implemented five additions: **(1) smiDE segmentation-contamination QC** (`overlap_ratio_qc.R`): per-gene × cell_subtype overlap ratio (self-expression / neighbor-other-type expression within 50µm), joined onto master as `contamination_ratio` — genes with ratio ≥ 1 are contamination-dominated in that cell type. **(2) Genome-wide smiDE per-cell NB GLMM** (`smide_de.R`): **primary DE** for exploratory work (pseudobulk DESeq2 is the conservative cross-check), using smiDE/nebula with `~ RankNorm(otherct_expr) + offset(log(totalcounts)) + condition + (1|sample_id)` — the neighbor-expression covariate absorbs segmentation error and the sample-level random effect handles pseudoreplication. Enters master as `readout_class="smiDE"` (37,854 rows: 950 genes × 15 cell types × 3 contrasts). **smiDE integration results (2026-08-14):** smiDE recovers 10× more hits than pseudobulk (5,272 vs 500 at padj<0.05); Spearman concordance ~0.57, sign agreement ~79%. **MBRT is not null at per-cell resolution** — 1,360 MBRT_vs_Ctrl hits (vs near-zero in pseudobulk). MBRT day-2 signal clusters in SASP/senescence (11/17 SASP genes significant: Il1b, Il1a, Il6, Cxcl1/2, Ccl2/20, Csf2, Vegfa, Timp1, Igfbp7), early fibrosis (Col1a1/1a2/5a2/6a3 in 5-6 cell types, weaker than SBRT), Hif1a hypoxia (5 cell types), and immunosuppressive markers (Arg1/Tigit UP, Ifi27/H2-Aa DOWN). Cdkn1a/p21 resolved: MBRT > SBRT in 6 cell types at per-cell level (padj to 2e-30 in macrophages via MBRT_vs_SBRT contrast); MBRT_vs_Ctrl significant only in Tumor (3e-11) and stroma_unresolved (0.015). **Direction clarification (2026-08-14):** both arms drive p21 DOWN vs control at day-2 in most cell types; the MBRT > SBRT contrast is driven by SBRT suppressing p21 much further (macrophages log2FC -1.48 SBRT_vs_Ctrl vs ~0 MBRT_vs_Ctrl), not MBRT upregulating it. Consistent with p53-mutant 4T1 survivorship at 48h (arrested cells cleared, survivors proliferating). Contamination caveat: smiDE-only hits have higher overlap ratios (median 1.28, 65% ≥1) than pseudobulk-only (0.88, 48%); top MBRT genes mostly contamination-dominated except Col1a1 and Hif1a. Master table 88,945 rows with smiDE integrated. Pseudobulk-vs-smiDE concordance analysis (`smide_concordance.R`) compares effect sizes, hit overlap, contamination-ratio enrichment in discordant hits, and confirmatory-hit survival (391/2,328 confirmatory rows pass smiDE). **(3) Neighborhood upgrade** (`niches.R`, `spatial_mixing.R`): kNN 20→50 per CosMx site recommendation, SPIN 50% random neighbor retention to reduce spatial autocorrelation, k-means (fixed K=6) → Mclust spherical Gaussian mixture (EII model, BIC K in 3:12, 5k subsample fit + predict for O(N) assignment). **(4) FOV signal-loss QC** (`qc_fov_signal.R`): per-FOV mean nCount_RNA vs slide median, flags ratio < 0.4 (>60% signal loss), verifies flagged FOVs do not concentrate in one arm. Report-only. **(5) InSituCor spatial gene-correlation discovery** (`insitucor_discovery.R`): de novo spatial gene-gene co-expression modules on 4h cells (k=100, max 100k subsample), independent of the curated pathway sets — discovery input for peak/valley signature work. **InSituCor `condvar` API bug:** the `condvar` argument is not recognized by the installed version; InSituCor excluded from pipeline pending resolution. GitHub-only R packages: `smiDE` (`Nanostring-Biostats/CosMx-Analysis-Scratch-Space/_code/smiDE`), `InSituCor` (`Nanostring-Biostats/InSituCor`); `r-mclust` added to conda env.

**Cross-timepoint parity gap (identified 2026-08-14).** smiDE, GSEA, pathway UCell test, niche frequency, spatial mixing, myeloid M1/M2, and substate composition are computed ONLY for `mutter02_day2` (48h). The `combined_4h` and `combined_4h_treated` cohorts have only pseudobulk DE (count_engine) and composition (lm_engine). Engine input files already contain all samples; the gap is purely in rule parameterization (niche/mixing/myeloid/substate engine rules hardcode `mutter02_day2`) and smiDE script hardcoding. Plan at `docs/plans/2026-08-14-cross-timepoint-parity.md` (7 tasks). See memory `project_cross_timepoint_parity`.

**Exploratory work** — `dev/if_channel_check/` contains peak/valley reconciliation work and IF channel diagnostics. Legacy exploratory layers 1-3 (`dev/peak_valley_analysis/`, `dev/mbrt_vs_sbrt/`) were retired and deleted (2026-08-13) — all read from superseded per-sample objects. The M01 4h peak/valley signature was resolved to artifact (2026-07-13, see `project_mbrt_mechanism_status` memory).

## Code Organization

- `spatial-rads.org` — main literate notebook (~3900 lines), all analysis + documentation. Top `* Plans` heading indexes active plan docs.
- `workflows/*.smk` — Snakemake workflows. `data_model.smk`, `preprocessing.smk` (tangled from org); the cross-sample layer is split into `aggregate_typing.smk` (typing, terminus `full_labels.parquet` + `merged.rds`) and `aggregate_differential.smk` (consumes those, terminus `results_master.tsv`), both tangled from org (split 2026-07-12, superseding the monolithic `aggregate.smk`).
- `scripts/*.R` — R steps invoked by snakemake rules. The `preprocessing.smk` steps are tangled from org; the `scripts/aggregate/*.R` steps are authored directly as standalone scripts (not in the org notebook). Standalone figure scripts `scripts/fig_*.R` (design grid, panel provenance, gene-set coverage, `fig_qc_arm_balance`, `fig_qc_reproducibility`, `fig_qc_panel_sparsity`, `fig_sample_tech_metrics`, `fig_provider_concordance`) are tangled from the notebook's Results headers and run by hand (NOT snakemake-wired); they source `~/repos/science/R/figure_schema.R`. Arm-level result figures (2026-07-13, `theme_scifig`, authored-direct not org-tangled, run by hand): `fig_composition_cohort_scatter` (experiment-level M01-vs-M02 census, 2-panel all/day-2), `fig_composition_arm` (per-arm stacked bars plus propeller forest), `fig_composition_arm_scatter` (MBRT/SBRT-vs-Ctrl proportion scatters), `fig_de_summary` (MDE-aware confirmatory-hit heatmap plus call_class effect-vs-MDE; replaces the retired `de_dotplot`/`de_heatmap`/`deg_summary_heatmap` orphans), `fig_pathway_arm` (primary-pathway UCell arm-effect heatmap, STING excluded at figure level though still present in `pathway_test_m02day2.tsv`). Gated by `scripts/verify_arm_tables.R` (asserts arm tables carry the locked 15-subtype roster, cells reconcile to 3,277,090). See memory `project_arm_level_figure_suite.md`. Rendering: the `spatial-rads` R env exists only on beast, so render figures there via ssh and let Syncthing return the PNG/SVG (memory `reference_pad_r_env_broken.md`).
- `docs/plans/plan-aggregate.md` — handoff doc for the next cross-sample / cross-dataset workflow build.
- `results/processing/` — small TSVs and reports from `preprocessing.smk` (qc_summary, probe_qc_report, contamination_qc, plots).
- `dev/if_channel_check/` — IF channel diagnostics + peak/valley reconciliation work.
- `config/spatial-rads-conda-env.yaml` — conda environment definition.
- `config/config.yaml` — workflow config (datadir, samplesheet, qc thresholds, normalize scale_factor).
- `config/pathway_gene_lists.yaml` — canonical pathway gene sets (IFN-I/II, DDR, STING).
- `config/comparisons.yaml` — curated comparison registry (12 comparisons: M02 day-2 arm contrasts, M01 n=1 descriptive time-course, gated within-tumor peak/valley). Resolved by `scripts/build_comparisons.R` → `results/data_model/comparisons.tsv` (adds realized n, distinct mice, model rank, resid_df, `inference_capable`, `gate`); git-HEAD freeze on tier+hypotheses. The intended parameter source for the differential layer (hookup deferred). Built 2026-07-12.
- `config/marker_sets_provenance.tsv` — per-lineage marker source + weak-separation flags; `scripts/build_marker_sets.R` → `results/data_model/marker_panel_coverage.tsv` (+ `fig_marker_coverage.R` figure). Coarse-lineage on-panel completeness + freeze checks, mirroring the pathway gene-set builder. Built 2026-07-12.
- `data/metadata.xlsx` — experimental design metadata (single source of truth). Relational sheets: `mice`/`datasets`/`slides`/`samples` plus `if_channels` (one row per dataset×morphology-IF stain). The `datasets` sheet carries dataset-level RNA-panel design (`panel_base`, `panel_custom`, `assay_type`=RNA, `panel_n`, `n_negprobe`, `n_falsecode`); `if_channels` captures the 5 CosMx morphology stains (DAPI/CD298.B2M/PanCK/CD45/G) with segmentation/lineage `role`. IF channels are morphology stains for segmentation+lineage, NOT a protein-expression assay. Built from a legacy flat source by `scripts/build_metadata_xlsx.R`.
- `data/metadata_schema.yaml` — Frictionless-style schema (PK/enum/FK/required) consumed by `make_data_model.R` for validation. One resource per xlsx sheet.
- `data/data_model.rda` — relational model (list of tibbles: mice/datasets/slides/samples/if_channels) emitted by `data_model.smk`; `results/data_model/samples.tsv` is the sample-grain denormalized join (dataset-grain dims like `if_channels` are intentionally not joined in).
- `data/sources/` — write-protected vendor documents (NanoString panel files, quotes).

## Tech Stack

- **R** (primary): Seurat v5, tidyverse, data.table, arrow, ComplexHeatmap, RANN, patchwork
- **Conda env**: `spatial-rads` (R ≥4.4, conda-forge + bioconda)
- **Compute**: jeff-beast (48 cores, 124 GB RAM, **NVIDIA RTX A4000 16 GB GPU**). Memory, not CPU, is the practical bound on Seurat operations over the merged ~3.32M-cell cohort. The GPU drives scVI integration (env `spatial-rads-scvi`), which trains GPU-direct — no CPU sketch-train workaround needed.
- **Data storage**: `/mnt/data/projects/spatial-rads/` (local disk; mirrored from GCS). Heavy intermediates (raw/qc/norm/typed/scored RDS, reference) live here; small TSVs + plots in `results/`.
- **Snakemake**: workflows under `workflows/*.smk`, driver in `basecamp` env, R steps via `conda run -n spatial-rads Rscript`. **Always set `TMPDIR=/mnt/data/projects/spatial-rads/tmp`** (not root `/tmp`). Per the Snakemake style guide, workflows use the per-rule `threads:` directive and rely on Snakemake's default `set -euo pipefail` — **no `shell.prefix`** (a workflow `shell.prefix` clobbers the default strict mode). **`rule all` is a manifest of ALL important run outputs (deliverables), not the minimum needed to trigger the DAG** — list co-outputs like `data_model.rda` explicitly even though a co-produced target already pulls them.

## Key Conventions

- **Snakemake invocation**: `TMPDIR=/mnt/data/projects/spatial-rads/tmp conda run -n basecamp snakemake -s workflows/<name>.smk --cores N`. Dry-run before execute (`--dry-run`).
- **Per-sample data layout** (post-`preprocessing.smk`): `processing/raw/{s}.raw.rds` → `processing/qc/{s}.qc.rds` → `processing/norm/{s}.norm.rds` (terminus; the `typed`/`scored` typing+pathway stages were removed 2026-07-10), all under `/mnt/data/projects/spatial-rads/`.
- **Cell typing**: M01 keeps Yi ImmGen labels with confident-epithelial `a`/`b`/NA cells relabeled to `tumor_epithelial`; M02 uses Seurat anchor TransferData against a 20k natural-abundance pooled-M01 cell reference. **For "tumor compartment" cuts, group `a` + `tumor_epithelial`** — M02 tumor cells land mostly in `a` faithfully to M01. *(These are the per-sample processing labels; `aggregate_typing.smk` re-types all cells at merged scale via scVI cluster-then-annotate — see Analysis Architecture — emitting coarse `tumor`/`stroma`/`immune` (plus tier-2 immune subtypes) directly, so the `a`-bucket grouping applies only to per-sample / legacy outputs. For cross-dataset work, use the **unified per-cell label table** at `results/aggregate/full_labels.parquet` (one row per cell: `compartment` = tumor/stroma/immune + final `cell_subtype` joining all three tiers, plus `subtype_source` and `rescued`; built by `scripts/aggregate/unify_labels.py`). Its per-tier components: aggregate coarse labels at `results/aggregate/full_coarse_labels.parquet`, immune subtypes at `/mnt/data/projects/spatial-rads/aggregate/full/immune_subtypes_rescued.parquet` (marker-rescued final; `immune_subtypes.parquet` is the pre-rescue SingleR-only intermediate), and stroma subtypes at `aggregate/full/stroma_subtypes_rescued.parquet` (marker-rescued final; `stroma_subtypes.parquet` is the pre-rescue UCell-only intermediate).)*
- **Exploratory R scripts** (legacy): `conda run -n spatial-rads Rscript dev/peak_valley_analysis/<script>.R`
- **Stats**: n=1 per condition in Mutter_01 → report effect sizes (log2FC, % expressed), NOT p-values. Mutter_02 brings replicates for formal inference at 2d.
- Peak/valley ground truth from H2AX IHC at 4h only.
- Stripe model: 15-deg tilt, 1.02 mm spacing, 4 peaks.
- Tongue and flank models analyzed separately.

## Key Contacts

- Yi Liu — CosMx data provider, cell type annotations, pathway scores
- Rob Mutter — Lab PI
- Jenn Fazzari — slide layout coordination

## Statistical Caveats

- All Mutter_01 analyses are descriptive (n=1). Formal testing awaits Mutter_02 replicates.
- 1000-gene panel has blind spots; negative results need cautious interpretation.
- "Signature persistence" at later timepoints may reflect cell turnover, not intrinsic memory.
